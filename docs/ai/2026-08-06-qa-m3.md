# M3 (volumes and activations) QA'd — Pass with follow-ups

2026-08-06, autonomous routine run. QA of milestone M3 of spec 0002.

Full QA record: [../specs/0002-agent-environment/qa/M3-volumes-and-activations.md](../specs/0002-agent-environment/qa/M3-volumes-and-activations.md).

## Done this run

- **No open PRs to triage** (`list_pull_requests` empty). Branched from `main` at #52.
- **M3 QA'd — Pass with follow-ups.** `pnpm check` green; `pnpm fixture:m3` all six checks PASS
  live over Docker. Attacked the boundaries the fixture does not cover:
  - `planMounts` refuses every single-field deviation of a mount request (subtree, broader
    subtree, mode bump to read-write, role bump to writer) — only the exact declared tuple admits.
  - Writer-planted symlinks inside a shared volume cannot escape a reader's subtree: `../secret`
    and `/volumes/shared/secret/…` both `ENOENT` in the reader's mount namespace; `/etc/hostname`
    resolves to the reader's _own container_ file, not the host or the volume. Subtree enforcement
    by bind-mounting the subtree is symlink-safe.
  - `resetSession` clears nested content (`nested/deep/a.txt`), returns the pre-reset digest, empty
    digest differs from populated.
  - Journal reconstructs from a single `volume.reset` record; all 7 promised events present; the
    two deliberate silences (undeclared-mount refusal, `EROFS` write denial) confirmed absent as
    the spec designs.

## Follow-up recorded for the M4/M5 executor (QA finding 1, Low, non-blocking)

`VolumeStore.hostPath` throws a raw `Error: EEXIST` (not `UndeclaredMountError`) when a writer has
planted a file where a reader's declared subtree directory belongs — surfaces in `mountRequestsFor`
before any container. Fails **closed** (correct), but the throw is untyped and unjournaled. The
spec's audit set has no start-refusal event, so the silence is consistent; the untyped throw is a
diagnosability gap. When the runtime gains a typed start-refusal path (M4/M5), type this throw and
surface a diagnostic. `packages/agent-environment/src/volumes.ts` `hostPath` / `mountRequestsFor`.

## Next run, per the routing table

**M3 is Complete and QA'd; M1–M3 done. Next: execute M4** (`wakeups-turns-leases`) via
[execute-a-milestone](../dev/workflows/execute-a-milestone.md). M4 carries a spec
runtime-feasibility risk (write-lease latency), so build the lease mechanism against a real fixture
early. None of the standing owner rulings below block M4.

## Standing items, carried forward from [2026-08-06-m3-execution-run.md](2026-08-06-m3-execution-run.md)

- **Branch protection**: owner should verify the ruleset does what they intend. One reminder
  warranted if unaddressed; none before 2026-08-10 (last reminder tracked in the M2 QA note).
- **ADR-0005 and ADR-0006 remain `Proposed`**; spec 0001's M1 is gated on those rulings.
- Spec 0002 open questions still hold two owner rulings (`model.infer` spend-meter boundary;
  ADR-0003 retention-scope amendment). Neither blocks M4.
- M2 QA findings 2–5 (bridge-path writer counting, credential-environment bridge targets,
  `allow-all` network bridges, two bridges on one mediated volume) await owner rulings; none block
  M4.

## Notification decision

None sent this run. Nothing needs owner action now: M3 passed QA, no draft awaits acceptance, no
setting blocked a merge, and the standing branch-protection reminder is not yet due (>= 2026-08-10).
The M3 execution run (2026-08-06) already reported it sent no notification.
