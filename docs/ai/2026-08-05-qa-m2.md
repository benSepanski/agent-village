# M2 (topology checker) QA'd — pass with follow-ups

2026-08-05, autonomous routine run (second of the day).

Done this run:

- **No open PRs to triage**; branched from main at #50.
- **M2 QA'd**: verification rerun exactly as written (`pnpm check`, `pnpm fixture:m2`, and a live
  `pnpm fixture:m1` over Docker — the SessionStart hook's dockerd works). Fourteen hostile variants
  beyond the fixtures. Verdict **Pass with follow-ups**:
  [qa/M2-topology-checker.md](../specs/0002-agent-environment/qa/M2-topology-checker.md).
- **One real defect found and fixed in the QA pass**: the checker accepted a `reader`-role mount
  with mode `read-write` — a second write path on a volume that the role-based writer count missed
  (the exact "same violation expressed differently" M2's approach section warned about). Fixed with
  a `mount-role-mode-mismatch` rule, an extended writer count, fixture variants, and unit tests.

Open for the owner (recorded as findings 2–5 in the QA doc; **none block M3**):

1. Should AC-1.4's "a volume another environment writes" include mediated-write **bridge** paths,
   not just mount writers? Recommend yes, via spec amendment.
2. Should a bridge from a non-credential-holding environment be allowed to target a
   credential-class mediated volume? Recommend no, but it is the owner's line to draw.
3. Surface `allow-all` on network bridges the way mediated-write `allow-all` is surfaced? Recommend
   yes, minor.
4. Two bridges writing one mediated volume — intended? Recommend confirming, no change.

Notification decision: **none sent this run** — no draft awaits acceptance, nothing blocks the next
pass (M3 execution), and the owner questions above are non-urgent. Standing items carried from
[2026-08-05-m2-execution-run.md](2026-08-05-m2-execution-run.md): branch-protection verification
(one reminder warranted if unaddressed, none before 2026-08-10); ADR-0005/0006 still `Proposed`,
gating spec 0001 M1; spec 0002's two open-question rulings.

Next run, per the routing table: spec 0002 is `Accepted` with unfinished milestones — **execute M3
(volumes and activations)**, which needs Docker (the SessionStart hook starts dockerd).
