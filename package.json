{
  "name": "agent-village",
  "version": "0.0.0",
  "private": true,
  "description": "Design repository for agent-village. No implementation yet — see docs/specs/.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "pnpm format:check && pnpm check:links",
    "check:links": "node scripts/check-doc-links.mjs",
    "format": "prettier --write \"**/*.{md,json,yml,yaml,mjs}\"",
    "format:check": "prettier --check \"**/*.{md,json,yml,yaml,mjs}\""
  },
  "devDependencies": {
    "prettier": "3.4.2"
  }
}
