{
  "name": "@agent-village/tests-structural",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "dependency-cruiser": "^16.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
